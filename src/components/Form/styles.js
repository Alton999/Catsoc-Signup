import {makeStyles} from '@material-ui/core/styles';

export default makeStyles((theme)=>({
    container: {
        width: '250px',
        margin: 'auto',
    },

    title: {
        textAlign: 'center',
        padding: '10px',
    },

    field: {
        marginBottom: '100px',
    },

    button: {
        width: '100px',
        padding: '10px',
    }
}))